# 🏗️ الهندسة المعمارية للنظام (System Architecture)

يقدم هذا المستند تحليلاً تقنياً لمكونات نظام تنزيل وتبييض المانجا وكيفية ترابط الأكواد والخدمات.

---

## 📐 التقسيم الرئيسي الهيكلي (Core Components)

ينقسم النظام إلى مكونين رئيسيين مستقلين في التشغيل ومعالجة البيانات:

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        🤖 bot-core Component                           │
│ ┌────────────────┐   ┌────────────────┐   ┌──────────────────────────┐ │
│ │  Discord Bot   │   │ Web Admin UI   │   │  Manga Downloader Engine │ │
│ └───────┬────────┘   └───────┬────────┘   └────────────┬─────────────┘ │
│         │                    │                         │               │
│         └────────────────────┼─────────────────────────┘               │
│                              ▼                                         │
│                   ┌──────────────────────┐                             │
│                   │  Database & Storage  │                             │
│                   └──────────────────────┘                             │
└──────────────────────────────┬─────────────────────────────────────────┘
                               │ (HTTP / REST API Calls)
                               ▼
┌────────────────────────────────────────────────────────────────────────┐
│                   🎨 inpainting-core Component                         │
│ ┌────────────────────────────────────────────────────────────────────┐ │
│ │ FastAPI + Gradio AI Microservice (ZeroGPU / RTX 6000 Blackwell)    │ │
│ │ ├─ Automatic Speech Bubble Mask Generator                          │ │
│ │ └─ LaMa Deep Inpainting Engine (HD Ghost-free Text Erase)          │ │
│ └────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 🔍 شرح الملفات ووظائفها التقنية

### 🤖 1. قسم الإدارة والتحميل (`bot-core/`)

#### 📄 `main.py`
- **الوظيفة**: النقطة الرئيسية لتشغيل بوت الديسكورد.
- **التفاصيل**: تهيئة `discord.ext.commands.Bot` تحميل الأوامر والـ Cogs من مجلد `cogs/` والتحقق من التوكن وصلاحيات البوت، وربط البوت بـ Asyncio Event Loop.

#### 📄 `web_panel.py`
- **الوظيفة**: خادم لوحة التحكم الإدارية (FastAPI + HTML5 UI).
- **التفاصيل**: عرض إحصائيات النظام الفورية (RAM, CPU, Active Downloads)، إدارة المستخدمين والرتب، ومتابعة سجلات البوت الحية.

#### 📄 `database.py`
- **الوظيفة**: الطبقة المركزية لإدارة قاعدة البيانات.
- **التفاصيل**: نماذج الجداول للمستخدمين (`users`)، السلاسل المتابعة (`tracked_series`)، والسجلات (`logs`)، مع دعم الاستعلامات غير الحاجبة.

#### 📄 `manga_downloader.py`
- **الوظيفة**: المحرك التنفيذي لتحميل الفصول.
- **التفاصيل**: استقبال طلبات التحميل من البوت أو لوحة الويب، الاتصال بالمحرك المناسب من `providers/` وتنفيذ عملية تنزيل وتجميع الصور.

#### 📄 `smart_stitch.py` & `drive_stitch.py`
- **الوظيفة**: معالجة الصور ورفعها.
- **التفاصيل**:
  - `smart_stitch.py`: قص وتجميع صفحات المانهوا عمودياً بدقة.
  - `drive_stitch.py`: رفع الصور والمستندات إلى Google Drive وإنشاء روابط المشاركة.

#### 📁 `providers/`
- يحتوي على محركات جلب واستخراج الفصول (مثل `asura_provider.py`, `comix_provider.py`, `kakao_provider.py`).
- يعتمد على `base_provider.py` لتوحيد الواجهة البرمجية، ويدعم مكتبة `scrapling` ومحركات `playwright` لتجاوز تحديات حماية Cloudflare.

#### 📁 `cogs/`
- `admin.py`: أوامر الإدارة والإحصائيات.
- `downloads.py`: أوامر تنزيل الفصول.
- `manga_cleaner.py`: أوامر تبييض الفصول بالذكاء الاصطناعي.
- `tracker_v3.py`: نظام رادار التتبع الدوري للفصول الجديدة.

#### 📁 `hf_worker/`
- خادم مخصص يمكن نشره كعامل بعيد على HuggingFace Spaces لتلقي أحمال العمل الكثيفة وتنفيذ عمليات التنزيل في البيئة السحابية.

---

### 🎨 2. قسم التبييض وإزالة النصوص (`inpainting-core/`)

#### 📄 `app.py`
- **الوظيفة**: خادم الذكاء الاصطناعي لمعالجة وتبييض المانجا.
- **التفاصيل**:
  - دمج `Gradio` مع `FastAPI` لتقديم واجهة تجريبية بالمتصفح وواجهات برمجية `REST API`.
  - دعم زينة `@spaces.GPU` للتوافق مع منصة HuggingFace ZeroGPU.
  - خوارزمية **Hard Mask Blending** لإزالة نصوص المانجا بدقة فائقة وبدون تشويه الخلفية.

---

## 🔄 مسار تدفق البيانات (Data Pipeline)

```text
[المستخدم] 
    │
    ├─> يُرسل أمر ديسكورد: !clean <رابط_الفصل>
    │
[bot-core]
    │
    ├── 1. استلام الطلب واستدعاء cogs/manga_cleaner.py
    ├── 2. استخدام providers/ لاستخراج جميع صور الفصل
    ├── 3. تحميل الصور إلى الذاكرة/الملفات المؤقتة
    ├── 4. إرسال الطلبات عبر HTTP POST إلى inpainting-core API
    │
[inpainting-core]
    │
    ├── 1. استلام الصورة وتوليد ماسك النصوص
    ├── 2. تطبيق نموذج LaMa على الـ GPU
    ├── 3. إرجاع الصور النظيفة المبيضة
    │
[bot-core]
    │
    ├── 5. استلام الصور وتطبيق الدمج الذكي smart_stitch.py
    ├── 6. رفع الفصل النهائي إلى Google Drive
    └── 7. إرسال رابط الفصل المبيض للمستخدم في الديسكورد 
```
